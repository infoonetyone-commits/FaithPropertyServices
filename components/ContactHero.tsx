import DockingWordHero from "@/components/DockingWordHero";

export default function ContactHero() {
  return (
    <DockingWordHero
      word="CONTACT"
      eyebrow="Contact Us"
      description="Get in touch to schedule a consultation or to request a quote for your project."
      videoSrc="/contact-hero-video.mp4"
    />
  );
}
